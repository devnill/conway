### Fixed

- **A child killed by its own budget now hands its parent a partial answer instead of a bare failed tool call** — board item `01M2V047Q99N4HGXF0Y42JVBB9`. The 80% runway warning was advice only: a child that spent its last step on more work died with everything still in its head, and all the parent received was `tool call failed`. Every terminal `BudgetExceeded` result now carries whatever the child actually said, followed by a derived note naming which ceiling ended it, how many steps and tool calls it got through, the last call it dispatched (the `max_steps` equivalent of what the deadline path already reported), and the child's own `transcript_ref` to resume from — all of it mechanically derived from what the runtime already knew, so it holds even when the model ignores its wrap-up notice entirely. Because the note travels in the result's `summary`, it reaches the awaited-spawn JSON, the fire-and-forget `ChildResultRecord` line, and the TUI notice alike.
