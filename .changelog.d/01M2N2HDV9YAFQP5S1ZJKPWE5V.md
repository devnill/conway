### Added

- **A selectable, editable context-window row for an already-configured model in `/settings` → providers** — board item `01M2N2HDV9YAFQP5S1ZJKPWE5V`, the UI entry point board item `01M23M2P79R5G28TPGG7PPJQ32` deliberately deferred (`App::apply_edit_model_context_window` was landed but left unwired). Select a configured provider's row and press `w` to open the SAME context-window card the add flow's own assumed-floor branch already uses, now pre-filled with the model's CURRENT resolved value and its provenance (`models.json` / `verified` / `probed` / `floor (assumed)` — the identical vocabulary `conway routes explain` prints, never a bare number). Reuses the add flow's own accept-vs-override rule unchanged: an empty `Enter` writes nothing (a real value stays live for a later, better probe); a typed number persists as a `.conway/models.json` `Override`, through the same tested write primitive, surviving any later, disagreeing probe. `docs/providers.md`'s "Editing an already-configured model's window" has the full account.
