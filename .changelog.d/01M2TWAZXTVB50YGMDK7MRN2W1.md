### Fixed

- **A `/model` or `/role` switch now NAMES what it switched to on its own `/agents` row** — board item `01M2TWAZXTVB50YGMDK7MRN2W1`. The switch collapses to one row for the live tip, as before, but that row read `* 01M2TVT0 agent fork @seq 12 (focused)` — naming everything about the switch except the model it was for. `commands::switch_session` now records its target in `AppState::spawn_role_or_model` beside the `switch_lineage` entry it already wrote, so the row (and the status line's lineage breadcrumb) shows `model: <backend/model>` or `role: <alias>` from the same value the confirmation notice is composed from. A row whose model nobody chose by hand is unchanged: this shows what an operator's own command NAMED, never a per-row re-resolution of what every agent routes to.
- **The `/agents` panel no longer indents a row under agents it is not showing** — same board item, second defect. The indent was measured against the real parent chain while the row set is filtered twice over (the terminal-status filter, and the switch-lineage collapse), so three `/model` switches left a lone row six columns in with nothing above it. The panel now indents by how many of a row's ancestors are themselves rows on screen; `/tree`, which prints every node, still indents by the real tree.
