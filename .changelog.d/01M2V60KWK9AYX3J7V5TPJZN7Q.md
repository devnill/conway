### Fixed

- **The status bar's elapsed counter now shows the real wait while a permission prompt is open, instead of a literal `0s`** — board item `01M2V60KWK9AYX3J7V5TPJZN7Q`. The figure was read from `turn_started_at`, which means "a model turn is in flight" and is therefore already cleared by the time a prompt for one of that turn's tool calls can open (the agent loop emits `TurnFinished` before any tool call is dispatched), so the `awaiting permission… 0s` line never moved no matter how long the operator had been sitting there — while the spinner glyph beside it kept advancing, which made the wrong number look live. The prompt now runs its own clock (`awaiting_permission_since`, started when the prompt opens and stopped when the decision lands), so the live figure agrees with the `waited_ms` the decision record persists and with the `allowed once · waited 53s` note conway already showed afterwards.
