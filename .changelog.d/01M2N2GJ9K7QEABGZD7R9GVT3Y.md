### Fixed

- **A model configured outside guided setup's own shortlist no longer runs a real turn on an assumed context-window floor in total silence** — board item `01M2N2GJ9K7QEABGZD7R9GVT3Y`. The prior item (`01M23M2P79R5G28TPGG7PPJQ32`) made every branch of the ADD FLOW confirm its resolved context window, but that flow only ever runs for a model conway itself offered — guided setup's own [`HOSTED_CHOICES`] shortlist (pinned to exactly one model per hosted provider, `ollama_cloud` → `glm-5.2`), or `/settings` → providers → add. A hand-edited `settings.json` `backends`/`roles` entry, a role-chain edit naming a second model on an already-configured provider, and `--model <backend>/<model>` naming something unconfigured all reach a real turn without ever touching that confirm step — the literal `ollama_cloud/glm-5.3` incident that motivated the parent item was never reachable through the fix that item shipped. conway now checks once more, at the one point every entry path shares regardless of how the model got configured: immediately before a session's first real turn. A model with no `models.json` entry, no live probe, and no verified dialect baseline now prints the same honest floor-number-and-remedy notice the add flow already prints — on `stderr` for a non-interactive run, and as a standing transcript entry for the interactive TUI — before the guess can cost a real run. No new resolver (the same `ContextTokensSource` the add flow and `conway routes explain` already use), no re-asking once a `models.json` entry exists for the pair, and no prompt in a non-interactive context.

### Changed

- **`HOSTED_CHOICES`' one-model-per-hosted-provider pinning stays as it is.** Investigated as part of this item (a curation decision with a real cost is worth re-checking, not assuming); ruled that it should not change silently as a side effect of this fix, and is left exactly as it was.
