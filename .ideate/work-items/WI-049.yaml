id: WI-049
type: work_item
title: TranscriptResolver with bounded LRU memoization
complexity: high
scope:
  - path: crates/conway-session/src/resolver.rs
    op: modify
  - path: crates/conway-session/tests/resolver_tests.rs
    op: create
depends:
  - WI-048
blocks: []
criteria:
  - Root session resolution returns its own records in seq order [machine]
  - Child resolution equals resolve(parent)[0..at_seq] ++ own_records, asserted element-wise on a 3-level chain [machine]
  - Depth-5 transitivity test matches an independent reference implementation [machine]
  - "Sharing: two same-point siblings share exactly one parent-prefix Arc (Arc::ptr_eq on peek_prefix) [machine]"
  - "Memoization: second resolve of the same (sid, at_seq) performs zero file reads [machine]"
  - "Bound: capacity-2 LRU evicts as expected (third key evicts first; re-resolve re-reads) [machine]"
  - "Snapshot: parent appends after fork never change resolve(child) [machine]"
  - "Cycle: mutually-referencing origins return CorruptAncestry, not an infinite loop; depth > 256 same [machine]"
  - "`resolve` returns Arc<[LogRecord]>, is async and Send [machine]"
  - "Property test (proptest, >=128 cases): random fork trees satisfy the length and prefix equalities [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Compute an agent's effective transcript by walking the ancestry chain with at_seq truncation, memoized so N siblings share one allocation — the in-memory counterpart of O(1) fork.

  Full binding implementation notes: docs/plan/wi-conway-session.md section WI-049.
resolution: null
status: done
cycle_created: 0
cycle_modified: 0
content_hash: 33e054e46e7361e6874e27156b04c2eba981a46e9b4e72fab4472dfcee3fdb11
token_count: 438
