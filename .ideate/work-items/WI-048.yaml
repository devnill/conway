id: WI-048
type: work_item
title: O(1) fork-by-reference
complexity: medium
scope:
  - path: crates/conway-session/src/fork.rs
    op: modify
  - path: crates/conway-session/tests/fork_tests.rs
    op: create
depends:
  - WI-047
blocks: []
criteria:
  - "`fork(parent, at, meta)` creates a child file whose total line count is exactly 1 [machine]"
  - Child header origin equals ForkOrigin{parent, at_seq, mode}; a None origin is filled from arguments [machine]
  - "Zero-copy: forking a 10,000-record parent yields child file < 2000 bytes with zero child records [machine]"
  - "O(1): fork reads 0 parent lines (instrumented counter) regardless of parent size [machine]"
  - "`fork` with at > head returns InvalidRange and creates no file; nonexistent parent returns NotFound and creates no file [machine]"
  - "Parent-immutability property test (proptest, >=128 cases): parent bytes unchanged by fork; parent appends after fork do not affect the child's resolved prefix [machine]"
  - "Sibling test: 10 same-point forks yield 10 distinct ids/files; parent byte-identical [machine]"
  - Child header fsynced before fork returns under all three FsyncPolicy values [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Implement SessionStore::fork as a single header write referencing (parent, at_seq, mode), copying nothing — the storage property that makes tournament fan-out affordable.

  Full binding implementation notes: docs/plan/wi-conway-session.md section WI-048.
resolution: null
status: done
cycle_created: 0
cycle_modified: 0
content_hash: 4de23fdb8bf8930a61bd685b39f58a08c30773b49c9b1a3f0b4f6aedd9999616
token_count: 415
