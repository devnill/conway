id: WI-103
type: work_item
title: Session resume, listing, and fork-from on Conway
complexity: medium
scope:
  - path: crates/conway/src/conway.rs
    op: modify
  - path: crates/conway/tests/resume.rs
    op: create
depends:
  - WI-050
  - WI-084
  - WI-100
  - WI-102
blocks: []
criteria:
  - resume(sid) returns a handle with matching id and header-recorded root; nonexistent sid errors naming it [machine]
  - Resumed transcript equals pre-restart records (write, drop, rebuild, resume, compare) [machine]
  - Resumed tree() reconstructs all agents from the index (1 root + 2 children fixture = 3 nodes) [machine]
  - Resumed prompt appends at head + 1 [machine]
  - Resume over a truncated trailing line succeeds with a warning through the event stream [machine]
  - sessions(filter) delegates to SessionStore::list unmodified [machine]
  - fork_from(sid, at, spec) creates the child via SessionStore::fork with correct ForkOrigin; at > head errors with both seqs; child file has exactly one line; at == 0 is valid with empty inherited prefix [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Complete the Conway surface with resume, listing, and CLI-independent fork-from support, so no CLI capability lacks a library equivalent (C-03, GP-05).

  Full binding implementation notes: docs/plan/wi-conway-facade.md section WI-103.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: 4d3d9904837d8fb47db3375dffc374b5eba424bf9ccd65a767b147181d31b07d
token_count: 359
