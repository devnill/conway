id: WI-035
type: work_item
title: HealthProber — periodic Backend::probe loop
complexity: medium
scope:
  - path: crates/conway-routing/src/prober.rs
    op: modify
depends:
  - WI-008
  - WI-031
  - WI-033
blocks: []
criteria:
  - "`HealthProber::spawn(backends, health, config) -> ProberHandle` exists [machine]"
  - "`ProberHandle` exposes idempotent non-blocking `shutdown()` and `async join()`; dropping without shutdown does not abort the task [machine]"
  - With tokio::time::pause, advancing by probe_interval triggers exactly one probe per backend per interval (5-interval counter test, no drift extras) [machine]
  - Ok(ProbeReport) records `Observation::Ok{latency}`; Err records `ProbeFail` [machine]
  - A probe exceeding probe_timeout is abandoned and records ProbeFail exactly once [machine]
  - Backends probed concurrently within a tick (~probe_timeout wall time for N backends) [machine]
  - A panicking probe is caught, records ProbeFail, and later ticks still probe that backend [machine]
  - probe_enabled=false exits immediately with zero observations [machine]
  - After shutdown(), no further observations across 10 advanced intervals [machine]
  - prober.rs never calls HealthRegistry::state (write-only) [machine]
  - "`cargo test -p conway-routing` passes [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Keep the Probe breaker fed with liveness data independently of request traffic, so a dead local server is distinguishable from a slow one before a request is routed to it.

  Full binding implementation notes: docs/plan/wi-conway-routing.md section WI-035.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: dc3228cdd10a34c030c04c6784c706423a1b76e80fe16002ed6658f9278854f2
token_count: 423
