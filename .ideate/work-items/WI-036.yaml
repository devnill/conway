id: WI-036
type: work_item
title: RoutingExplain — ExplainReport for chosen and skipped candidates
complexity: medium
scope:
  - path: crates/conway-routing/src/explain.rs
    op: modify
  - path: crates/conway-routing/tests/explain_report.rs
    op: create
depends:
  - WI-008
  - WI-034
blocks: []
criteria:
  - "`RoutingExplain::new(&DeclarativeRouter)` and synchronous no-I/O `explain(&RouteRequest) -> ExplainReport` exist [machine]"
  - ExplainReport and nested types derive Serialize/Deserialize/Debug/Clone/PartialEq with serde round-trip test [machine]
  - explain and resolve agree by construction over >=12 scenarios (Selected entries equal resolve output; NoCandidate implies zero Selected) [machine]
  - entries.len() == chain.len() for non-pinned requests; == 1 for pinned [machine]
  - Each entry carries the router's exact RoutingReason, a BreakerSnapshot read at explain time, and Option<CapabilitySummary> [machine]
  - explain never calls HealthRegistry::record (counting fake, 100 calls) [machine]
  - "`ExplainReport.headroom_tokens` equals HeadroomPolicy::resolve(role) for both overridden and default roles [machine]"
  - A headroom-skipped entry renders with its shortfall string; golden file includes a headroom-rejected candidate byte-for-byte [machine]
  - "`render_text` header line is exactly `role: planner (est_tokens=34000, headroom_tokens=16384)` [machine]"
  - "`cargo test -p conway-routing` passes including tests/explain_report.rs [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Answer 'why did this model run, and why not the others' as a serializable report, so `conway routes explain <role>` and the CLI's /why render from one source of truth — including the effective headroom reservation.

  Full binding implementation notes: docs/plan/wi-conway-routing.md section WI-036. HEADROOM AMENDMENT applies — see docs/plan/wi-conway-routing-amendment.md section WI-036; amended text supersedes the original where they differ.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: faaef3b22a71667a737bb08c8cebbf6a1be69a5dd2b55bf698559d54df2847e0
token_count: 517
