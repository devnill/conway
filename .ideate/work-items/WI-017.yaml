id: WI-017
type: work_item
title: ModelMetadata loader and per-(backend, model) capability construction
complexity: medium
scope:
  - path: crates/conway-backends/src/model_metadata.rs
    op: create
  - path: crates/conway-backends/src/capabilities.rs
    op: create
  - path: crates/conway-backends/tests/model_metadata.rs
    op: create
  - path: crates/conway-backends/tests/fixtures/models.toml
    op: create
depends:
  - WI-008
  - WI-016
blocks: []
criteria:
  - "`ModelMetadataStore::load(&Path)` on the fixture returns every entry; field-by-field equality asserted for at least two entries [machine]"
  - Load on a nonexistent path returns `Ok(empty())`, not an error [machine]
  - Load on a syntactically invalid file returns `Err(ConfigError::Metadata)` [machine]
  - Lookup order is exact model_id, then normalized (lowercased, `:`/`/` collapsed to `-`, `-latest` stripped), then None; asserted with `Qwen3-Coder:30b` matching `qwen3-coder-30b` [machine]
  - "`build_capabilities` with no metadata and no overrides returns `reliability_tier: Unknown`, `tool_calling: NonStreamingOnly`, dialect-default `max_context_tokens` and `CacheMode` [machine]"
  - "`stream_tools` default is true for Verified, false for Community/Unknown; explicit override wins for all tiers [machine]"
  - "`build_capabilities` is a pure function; no io imports in capabilities.rs [machine]"
  - No `reqwest`/`std::fs`/`tokio::fs` reference in capabilities.rs [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: The single place where 'capabilities are per (backend, model), never per backend' (module boundary rule) is realized.

  Full binding implementation notes: docs/plan/wi-conway-backends.md section WI-017.
resolution: null
status: done
cycle_created: 0
cycle_modified: 0
content_hash: 29387c9c81ebd2fd6c268abc01a376a14fd160389c62930aa23b1092b91fcf96
token_count: 473
