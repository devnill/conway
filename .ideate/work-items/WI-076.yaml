id: WI-076
type: work_item
title: Crate scaffold, module skeleton, and EventBus
complexity: medium
scope:
  - path: crates/conway-runtime/Cargo.toml
    op: create
  - path: crates/conway-runtime/src/lib.rs
    op: create
  - path: crates/conway-runtime/src/error.rs
    op: create
  - path: crates/conway-runtime/src/events.rs
    op: create
  - path: crates/conway-runtime/src/context/mod.rs
    op: create
  - path: crates/conway-runtime/src/context/builder.rs
    op: create
  - path: crates/conway-runtime/src/context/prefix.rs
    op: create
  - path: crates/conway-runtime/src/context/report.rs
    op: create
  - path: crates/conway-runtime/src/permission.rs
    op: create
  - path: crates/conway-runtime/src/tools/mod.rs
    op: create
  - path: crates/conway-runtime/src/tools/registry.rs
    op: create
  - path: crates/conway-runtime/src/tools/runner.rs
    op: create
  - path: crates/conway-runtime/src/attempt.rs
    op: create
  - path: crates/conway-runtime/src/agent_loop.rs
    op: create
  - path: crates/conway-runtime/src/runtime.rs
    op: create
  - path: crates/conway-runtime/src/tree.rs
    op: create
  - path: crates/conway-runtime/src/supervisor.rs
    op: create
  - path: crates/conway-runtime/src/subagent.rs
    op: create
  - path: crates/conway-runtime/src/mailbox.rs
    op: create
  - path: crates/conway-runtime/src/step_digest.rs
    op: create
  - path: crates/conway-runtime/src/result.rs
    op: create
  - path: crates/conway-runtime/tests/events_ordering.rs
    op: create
depends:
  - WI-008
blocks: []
criteria:
  - "`cargo build -p conway-runtime` succeeds with the documented dependency set [machine]"
  - "`cargo tree -i conway-backends` and `-i conway-tools` return no path (runtime names no concrete adapter or plugin crate) [machine]"
  - lib.rs declares every module in scope; stubs compile with no public items beyond those introduced later [machine]
  - "`EventBus::new(capacity)` and `emit(session, agent, event) -> u64` exist [machine]"
  - 8 tasks x 500 events for one session yield seqs exactly 0..4000, no duplicates or gaps [machine]
  - Two sessions maintain independent monotonic seq counters from 0 [machine]
  - Slow subscriber receives `Event::Lagged{skipped > 0}`; emit never blocks or errors [machine]
  - "`BusSink` implements EventSink and stamps envelopes with its bound (session, agent) [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Create the crate, fix the module layout so no two later work items must edit lib.rs, and implement the event bus that every other component emits through.

  Full binding implementation notes: docs/plan/wi-conway-runtime.md section WI-076.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: b4985ac4418df121f509be26902adad2d1d92e4f4e6418efd26fe08a380c4a13
token_count: 688
