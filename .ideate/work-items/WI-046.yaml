id: WI-046
type: work_item
title: conway-session crate skeleton, session metadata types, and JSONL line codec
complexity: medium
scope:
  - path: crates/conway-session/Cargo.toml
    op: create
  - path: crates/conway-session/src/lib.rs
    op: create
  - path: crates/conway-session/src/meta.rs
    op: create
  - path: crates/conway-session/src/codec.rs
    op: create
  - path: crates/conway-session/src/store.rs
    op: create
  - path: crates/conway-session/src/fork.rs
    op: create
  - path: crates/conway-session/src/resolver.rs
    op: create
  - path: crates/conway-session/src/index.rs
    op: create
  - path: crates/conway-session/src/provenance.rs
    op: create
  - path: crates/conway-session/tests/codec_tests.rs
    op: create
depends:
  - WI-008
blocks: []
criteria:
  - "`crates/conway-session` is a workspace member; `cargo check -p conway-session` succeeds [machine]"
  - "`SessionMeta`, `ForkOrigin`, `SessionStatus`, `SessionFilter` are public with the exact field sets from the implementation notes, Serialize + Deserialize + Clone + Debug + PartialEq [machine]"
  - "`codec::encode_header` produces a single-line header JSON round-trippable through `decode_header`, asserted on the architecture §5.1 example verbatim [machine]"
  - "`codec::encode_record(&LogRecord, seq)` emits exactly one newline-terminated line with top-level `seq` and `kind` keys [machine]"
  - "`codec::decode_line` distinguishes Header vs Record; a record line missing seq returns `CodecError::MissingSeq` [machine]"
  - "Property test (proptest, >=256 cases): decode(encode(r, s)) == (s, r) for arbitrary LogRecord [machine]"
  - "`cargo clippy -p conway-session -- -D warnings` passes [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Establish the crate, the persisted metadata schema, and the line-level (de)serialization every other item in this module depends on. Lays down the module skeleton with exact stub signatures so no two later work items edit the same file.

  Full binding implementation notes: docs/plan/wi-conway-session.md section WI-046.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: fca6339b16094cdf2aa526517224fb9d3291ac621559d4dd3a59ea450219772e
token_count: 541
