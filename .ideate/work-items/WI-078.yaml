id: WI-078
type: work_item
title: PermissionBroker — decision cache over the consumer's PermissionGate
complexity: medium
scope:
  - path: crates/conway-runtime/src/permission.rs
    op: modify
  - path: crates/conway-runtime/tests/permission_broker.rs
    op: create
depends:
  - WI-008
  - WI-076
blocks: []
criteria:
  - "`PermissionBroker::new(gate, bus)` and `async decide(&PermissionCtx, &ToolCall) -> PermissionOutcome {Allow | Deny{rendered_error}}` exist [machine]"
  - "AllowAlways{Session} caches: identical second call hits the gate zero times [machine]"
  - AllowAlways{Agent} caches per-agent only; sibling agents re-consult the gate [machine]
  - AllowAlways{AgentSubtree} honored for descendants (agent_path prefix match), not others [machine]
  - AllowOnce is never cached [machine]
  - Deny carries the reason and is not cached [machine]
  - DenyWithFeedback marked for model-visible tool error, not abort [machine]
  - Exactly one PermissionRequested precedes exactly one PermissionResolved per decision, in seq order [machine]
  - Dropped/cancelled gate future yields Deny{cancelled} and still emits PermissionResolved [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Layer a per-session decision cache above the embedder's PermissionGate and normalize gate outcomes into allow/deny with complete event emission.

  Full binding implementation notes: docs/plan/wi-conway-runtime.md section WI-078.
resolution: null
status: done
cycle_created: 0
cycle_modified: 0
content_hash: 71462658e369bae83738f2274f62859e273a14553a1c2d3ac4ba20760e1ffb3e
token_count: 403
