id: WI-101
type: work_item
title: SessionHandle core surface, TurnHandle, and EventStream
complexity: medium
scope:
  - path: crates/conway/src/session_handle.rs
    op: create
  - path: crates/conway/src/event_stream.rs
    op: create
  - path: crates/conway/src/lib.rs
    op: modify
  - path: crates/conway/tests/session_handle.rs
    op: create
depends:
  - WI-047
  - WI-082
  - WI-100
blocks: []
criteria:
  - SessionHandle exposes id, root, prompt -> TurnHandle, events, tree, context_report, transcript [machine]
  - SessionHandle is Clone + Send + Sync (compile-time assertion) [machine]
  - prompt passes text byte-for-byte to Runtime::prompt [machine]
  - events() filters to this session; a two-session test asserts isolation [machine]
  - events_from(seq) replays persisted then switches to live with no duplicates or gaps at the junction (10+5 test) [machine]
  - Slow consumers observe Lagged and continue receiving [machine]
  - EventStream implements futures_core::Stream<Item=Envelope>, Send + Unpin [machine]
  - TurnHandle::text/result/events behave as documented; result resolves on BudgetExceeded and Cancelled too [machine]
  - context_report returns fixed-order segments each with provenance [machine]
  - transcript returns the ancestry-resolved effective transcript (inherited prefix present for forked fixtures); unknown agent errors naming the id [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Deliver the Slice 1 consumer-facing handle: prompt in, events and text out, plus the GP-10 introspection reads. All methods are thin delegations to Runtime.

  Full binding implementation notes: docs/plan/wi-conway-facade.md section WI-101.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: 9a9ac36824e7230cc44db32cd9a680044f58e8145d176819bcdc45d0615abd86
token_count: 442
