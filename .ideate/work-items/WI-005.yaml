id: WI-005
type: work_item
title: Define agent result, budget, subagent spec, and agent/skill definition types
complexity: medium
scope:
  - path: crates/conway-core/src/agent.rs
    op: create
  - path: crates/conway-core/src/config.rs
    op: create
  - path: crates/conway-core/src/log.rs
    op: modify
depends:
  - WI-002
  - WI-003
  - WI-004
blocks: []
criteria:
  - "`agent.rs` exports `AgentResult`, `ResultStatus`, `Fact`, `Budget`, `SubagentSpec`, `SubagentMode` (re-export from `log`), `AgentDefRef`, `ToolSelector`, `AgentTreeSnapshot`, `AgentNode`, `AgentMessage`, `MessageKind`, `PermissionRequest`, `PermissionDecision`, `PermissionDecisionKind`, `PermissionScope` [machine]"
  - "`config.rs` exports `AgentDef`, `SkillDef`, `ConwayConfig` [machine]"
  - "`ResultStatus` includes `Rejected { missing: Vec<String> }` and `BudgetExceeded` variants; a unit test asserts all five variants round-trip through `serde_json` [machine]"
  - "`AgentResult::summary` is `String` and `AgentResult::new` truncates to DEFAULT_SUMMARY_LIMIT (2000) on a char boundary; a test with a 5000-char multi-byte summary asserts exactly 2000 chars, no panic [machine]"
  - "`Budget` has non-`Option` `max_steps` with `Default` yielding 40; asserted by test [machine]"
  - "`ToolSelector::selects(&ToolName) -> bool` exists; unit tests cover `All`, `Only`, `Except`, and a `*`-suffix pattern [machine]"
  - "`log.rs` gains `AgentResultRecord { seq, ts, result: AgentResult }` with tag `agent_result` [machine]"
  - "`cargo test -p conway-core` passes [machine]"
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Define the terminal `AgentResult` contract that carries the MAST mitigations (bounded summary, typed facts, artifacts, `transcript_ref`, `Rejected{missing}`), the subagent spec that makes fork and spawn exactly two modes, the parent-child message enum, the permission request/decision types, and the serde-only agent/skill definition types.

  Full binding implementation notes: docs/plan/wi-conway-core.md section WI-005. Note: depends on WI-003 (added during reconciliation) because both modify log.rs and must be sequenced.
resolution: null
status: done
cycle_created: 0
cycle_modified: 0
content_hash: bb83e9f788eca5083225f4911af4f96260e759cf594e0b951141596068bb70a7
token_count: 578
