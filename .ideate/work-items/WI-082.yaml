id: WI-082
type: work_item
title: Runtime facade — RuntimeDeps, start_root, prompt, cancel, subscribe, live context_report
complexity: medium
scope:
  - path: crates/conway-runtime/src/runtime.rs
    op: modify
  - path: crates/conway-runtime/tests/runtime_api.rs
    op: create
depends:
  - WI-081
blocks: []
criteria:
  - "`Runtime::new(RuntimeDeps)` exists with every field a port type; RuntimeDeps includes `headroom: Arc<HeadroomPolicy>` [machine]"
  - RuntimeDeps constructible entirely from conway-core fakes; test crate depends on neither conway-backends nor conway-tools [machine]
  - "`start_root` creates a session, appends the header, spawns one task, returns before the first turn completes [machine]"
  - "`prompt` appends UserTurn with Provenance::UserPrompt BEFORE returning; unknown agent returns AgentNotFound [machine]"
  - "`cancel` trips the agent's token; task terminates with Cancelled [machine]"
  - "`subscribe` yields post-subscription envelopes; concurrent subscribers observe identical seq sequences [machine]"
  - "`context_report` returns the most recent turn's report for a live agent [machine]"
  - "`tree()` returns started root agents (children arrive in WI-083) [machine]"
  - Two concurrent roots run in independent tasks with per-session monotonic events [machine]
  - With empty per_role and default_tokens 4096, agents produce RouteRequest.required.min_context == est_tokens + 4096 [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Expose the runtime's public surface over the agent loop and own agent-task lifecycle for root agents.

  Full binding implementation notes: docs/plan/wi-conway-runtime.md section WI-082. HEADROOM AMENDMENT applies — see docs/plan/wi-conway-runtime-amendment.md section WI-082 (adds the headroom field to RuntimeDeps).
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: 7b9ab8abf90081b0cbb124d48f85edb0cfbce98bf15fcdd720b1aa3443b06301
token_count: 473
