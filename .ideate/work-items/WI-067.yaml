id: WI-067
type: work_item
title: builtin_plugins() registry and cross-plugin integration tests
complexity: low
scope:
  - path: crates/conway-tools/src/registry.rs
    op: modify
  - path: crates/conway-tools/tests/builtins.rs
    op: create
depends:
  - WI-063
  - WI-064
  - WI-065
  - WI-066
blocks: []
criteria:
  - "builtin_plugins() returns exactly four plugins: conway.fs, conway.report, conway.shell, conway.subagent [machine]"
  - Union of tools is exactly 11 with the documented sorted name list [machine]
  - No duplicate tool names across plugins [machine]
  - Every spec().schema is a valid JSON Schema object with type object and properties [machine]
  - Every description non-empty and <= 1024 chars [machine]
  - "Cancellation conformance: all 11 tools return Err(Cancelled) on a pre-cancelled ctx (table-driven) [machine]"
  - "Truncation conformance: each tool's declared policy matches the documentation table [machine]"
  - cargo test -p conway-tools passes; no conway-runtime in cargo tree [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Assemble the four built-in plugins into the single registration entry point the facade consumes, and enforce the crate-wide conformance rules with one test suite.

  Full binding implementation notes: docs/plan/wi-conway-tools.md section WI-067.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: 8baad537457748c1be24f4a8acc39ea8c936c6cd8f1efcb5370faf2bc15cdc8e
token_count: 352
