id: WI-086
type: work_item
title: AgentResult construction, result contract validation, and MAST mitigations
complexity: high
scope:
  - path: crates/conway-runtime/src/result.rs
    op: modify
  - path: crates/conway-runtime/src/step_digest.rs
    op: modify
  - path: crates/conway-runtime/src/agent_loop.rs
    op: modify
  - path: crates/conway-runtime/tests/result_contract.rs
    op: create
  - path: crates/conway-runtime/tests/step_digest.rs
    op: create
depends:
  - WI-085
blocks: []
criteria:
  - ResultBuilder::from_report_tool and from_trailing_text exist; a report tool call takes precedence [machine]
  - Summary always non-empty, truncated to 2000 chars with elision marker; empty-output agents get a status-naming summary [machine]
  - Every terminal path produces an AgentResult with transcript_ref, usage, and steps_taken populated [machine]
  - "result_contract: failing structured output retried exactly once with the validation error injected as a system note; second failure yields Rejected{missing} enumerating failing schema paths [machine]"
  - "Valid structured output first time: zero retries [machine]"
  - transcript_ref content never auto-injected into the parent's context [machine]
  - "StepDigest: blake3(tool || canonical args); 3rd identical digest emits exactly one RepeatedStep and one SystemNote citing the prior seq [machine]"
  - 4th/5th identical calls emit nothing more for that digest; distinct digests tracked independently [machine]
  - "Argument normalization: key order does not change the digest; value changes do [machine]"
  - Digest ring bounded (64, LRU) over 10000 calls [machine]
domain: null
phase: null
work_item_type: feature
notes: |-
  Objective: Finalize the AgentResult contract and implement the three MAST mitigations the runtime owns: repeated-step detection, schema-enforced result contracts with a single corrective retry, and Rejected{missing} as a first-class terminal status.

  Full binding implementation notes: docs/plan/wi-conway-runtime.md section WI-086.
resolution: null
status: pending
cycle_created: 0
cycle_modified: null
content_hash: 8f44a0e30bee4c1f1cca01bc267c0481c40975ce26bae2a14d11e36f6c17226a
token_count: 526
